const
     identical = require ( './identical' )
   , change    = require ( './change'    )
   , same      = require ( './same'      )
   , different = require ( './different' )
   , missing   = require ( './missing'   )
   ;


module.exports = {
              identical
            , change
            , same
            , different
            , missing
        }